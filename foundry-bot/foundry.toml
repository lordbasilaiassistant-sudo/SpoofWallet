[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.26"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

[rpc_endpoints]
base = "https://mainnet.base.org"
base_fork = "http://localhost:8546"

[etherscan]
base = { key = "${BASESCAN_API_KEY}", chain = "base" }
